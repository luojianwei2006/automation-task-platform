server:
  port: 8084
  tomcat:
    max-swallow-size: 10MB

spring:
  application:
    name: task-admin-api
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://118.145.198.135:3306/task_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: task_user
    password: Task@2026!
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      validation-query: SELECT 1

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB

  data:
    redis:
      host: 192.168.10.11
      port: 6379
      database: 1
      timeout: 5000ms
      password: metaverse
      lettuce:
        pool:
          max-active: 8
          min-idle: 0

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT配置（与 Gateway、其他微服务共享密钥）
jwt:
  secret: TaskPlatformSecretKey2026@AutoTask#$%^&*()_+1234567890

logging:
  level:
    com.task.platform: DEBUG
    org.springframework.security: INFO

internal:
  api-token: task-internal-2026

pay:
  api-base-url: http://localhost:8087

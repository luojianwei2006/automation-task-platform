# 自动化任务平台 · 一期MVP开发指导文档 v1.0

> 制定日期：2026-05-02
> 依据文档：《PRD_自动化任务平台_v1.0.md》v1.1 + 《开发阶段计划_v1.0》
> 适用阶段：第1-16周
> 状态：✅ 开发团队执行指导文档

---

## 一、技术架构详细设计

### 1.1 系统分层架构

```
┌─────────────────────────────────────────────────────┐
│                  安卓客户端（Kotlin）               │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │  UI层     │ ViewModel│ Repository│  网络层   │  │
│  │(Compose)  │  (MVVM)  │  (数据源)  │(Retrofit) │  │
│  └──────────┴──────────┴──────────┴──────────┘  │
└──────────────────────┬──────────────────────────────┘
                       │ HTTPS / JSON
┌──────────────────────▼──────────────────────────────┐
│             API Gateway（鉴权·限流·路由）           │
└──────────────────────┬──────────────────────────────┘
          ┌────────────┼────────────┐
          ▼            ▼            ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │用户服务   │ │任务服务   │ │支付服务   │
   └──────────┘ └──────────┘ └──────────┘
          ┌────────────┼────────────┐
          ▼            ▼            ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │  MySQL   │ │  Redis   │ │ RabbitMQ │
   └──────────┘ └──────────┘ └──────────┘
```

### 1.2 后端目录结构规范

```
task-platform/
├── task-common/          # 公共模块
├── task-gateway/        # API网关
├── task-user-service/   # 用户服务
├── task-task-service/   # 任务服务
├── task-pay-service/    # 支付服务
├── task-admin-api/      # 管理后台API
└── task-job/           # 定时任务
```

### 1.3 安卓端目录结构规范

```
app/src/main/java/com/task/platform/
├── ui/
│   ├── login/           # 登录注册
│   ├── task/            # 任务大厅
│   ├── earnings/        # 收益中心
│   ├── profile/         # 个人中心
│   └── settings/        # 设置（含自动化模式）
├── network/             # Retrofit封装
├── repository/          # 数据仓库
├── model/               # 数据模型
├── service/             # 无障碍服务
└── utils/               # 工具类
```

> 状态：✅ 开发团队执行指导文档

---

## 二、数据库设计（12张核心表）

### 2.1 用户表（t_user）

```sql
CREATE TABLE `t_user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `phone` VARCHAR(20) NOT NULL UNIQUE,
  `password` VARCHAR(128) NOT NULL COMMENT 'BCrypt加密',
  `nickname` VARCHAR(32),
  `avatar_url` VARCHAR(512),
  `real_name` VARCHAR(64),
  `id_card` VARCHAR(128) COMMENT '加密存储',
  `real_auth_status` TINYINT DEFAULT 0 COMMENT '0未认证1审核中2已认证3失败',
  `wechat_account` VARCHAR(128),
  `alipay_account` VARCHAR(128),
  `invite_code` VARCHAR(16) UNIQUE,
  `inviter_id` BIGINT,
  `device_fp` VARCHAR(128),
  `status` TINYINT DEFAULT 1 COMMENT '0封禁1正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_phone (`phone`),
  INDEX idx_invite_code (`invite_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.2 用户收益明细表（t_user_earnings）

```sql
CREATE TABLE `t_user_earnings` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `related_id` BIGINT COMMENT '关联任务记录ID',
  `type` TINYINT NOT NULL COMMENT '1任务奖励2广告奖励3邀请返佣',
  `amount` DECIMAL(10,2) NOT NULL,
  `status` TINYINT DEFAULT 0 COMMENT '0待审核1已到账2已撤销',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user_id (`user_id`),
  INDEX idx_created_at (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.3 任务表（t_task）

```sql
CREATE TABLE `t_task` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `merchant_id` BIGINT NOT NULL,
  `title` VARCHAR(128) NOT NULL,
  `platform` TINYINT NOT NULL COMMENT '1抖音2小红书',
  `task_type` TINYINT NOT NULL COMMENT '1点赞2评论',
  `target_url` VARCHAR(1024) NOT NULL,
  `reward_amount` DECIMAL(10,2) NOT NULL,
  `total_quota` INT NOT NULL COMMENT '总完成次数上限',
  `used_quota` INT DEFAULT 0,
  `status` TINYINT DEFAULT 0 COMMENT '0待审核1已上架2已暂停3已结束4已拒绝',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_merchant_id (`merchant_id`),
  INDEX idx_status (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.4 用户任务记录表（t_user_task_record）

```sql
CREATE TABLE `t_user_task_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `task_id` BIGINT NOT NULL,
  `screenshot_url` VARCHAR(512),
  `submit_count` TINYINT DEFAULT 1,
  `status` TINYINT DEFAULT 0 COMMENT '0待审核1通过2拒绝',
  `review_result` VARCHAR(256),
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_task (`user_id`, `task_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.5 其他核心表（简版）

- `t_merchant`：商户表（含账户余额、认证状态）
- `t_recharge_record`：充值记录表
- `t_withdraw_record`：提现记录表
- `t_ad`：广告表
- `t_ad_record`：用户广告观看记录表
- `t_invite_relation`：邀请关系表
- `t_notification`：消息通知表
- `t_audit_log`：审核日志表

> 完整建表SQL见《数据库设计文档》

---

## 三、API接口详细设计

### 3.1 认证模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 发送验证码 | POST | `/api/v1/auth/sms/send` | 请求体：`{phone, type}` type: 1注册2登录3重置密码 |
| 用户注册 | POST | `/api/v1/auth/register` | 请求体：`{phone, code, password, nickname, invite_code}` |
| 密码登录 | POST | `/api/v1/auth/login` | 返回JWT Token |
| 验证码登录 | POST | `/api/v1/auth/login/sms` | 返回JWT Token |
| 重置密码 | POST | `/api/v1/auth/password/reset` | 验证码校验后重置 |

**JWT Token 规范**：
- Header：`Authorization: Bearer {token}`
- 有效期：30天（可刷新）
- 刷新机制：过期前7天内可凭refresh_token换新token

### 3.2 任务模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 任务列表 | GET | `/api/v1/tasks?platform=&type=&sort=` | 支持分页、筛选、排序 |
| 任务详情 | GET | `/api/v1/tasks/{id}` | 返回任务完整信息 |
| 接受任务 | POST | `/api/v1/tasks/{id}/accept` | 幂等设计，重复调用返回已有记录 |
| 提交任务 | POST | `/api/v1/tasks/{id}/submit` | multipart/form-data上传截图 |
| 我的任务记录 | GET | `/api/v1/tasks/records` | 分页返回我的接单记录 |

### 3.3 收益与提现模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 收益概览 | GET | `/api/v1/earnings/summary` | 累计收益、可提现、今日收益 |
| 收益明细 | GET | `/api/v1/earnings/records` | 分页，支持按类型筛选 |
| 申请提现 | POST | `/api/v1/withdraw/apply` | `{amount, channel, account}` |
| 提现记录 | GET | `/api/v1/withdraw/records` | 分页返回提现历史 |
| 绑定收款账户 | POST | `/api/v1/wallet/bind` | `{type, account}` type: 1微信2支付宝 |

---

## 四、安卓端开发指南

### 4.1 技术栈与版本

| 技术 | 版本 | 用途 |
|------|------|------|
| Kotlin | 1.9+ | 主开发语言 |
| Jetpack Compose | 1.6+ | UI框架 |
| MVVM架构 | — | 架构模式 |
| Hilt | — | 依赖注入 |
| Retrofit2 | 2.9+ | 网络请求 |
| Coil | 2.5+ | 图片加载 |
| DataStore | — | 本地存储（替代SharedPreferences） |

### 4.2 网络层封装示例

```kotlin
// network/ApiService.kt
interface ApiService {
    @POST("api/v1/auth/login")
    suspend fun login(@Body req: LoginRequest): ApiResponse<LoginResponse>

    @GET("api/v1/tasks")
    suspend fun getTasks(
        @Query("platform") platform: Int?,
        @Query("page") page: Int,
        @Query("size") size: Int = 20
    ): ApiResponse<PageResponse<TaskDTO>>
}

// network/ApiClient.kt
object ApiClient {
    private const val BASE_URL = "https://api.taskplatform.com/"

    fun create(): ApiService {
        val okHttpClient = OkHttpClient.Builder()
            .addInterceptor(AuthInterceptor())
            .connectTimeout(30, TimeUnit.SECONDS)
            .build()
        return Retrofit.Builder()
            .baseUrl(BASE_URL)
            .client(okHttpClient)
            .addConverterFactory(GsonConverterFactory.create())
            .build()
            .create(ApiService::class.java)
    }
}
```

### 4.3 无障碍服务核心代码（半自动引导模式）

```kotlin
// service/TaskAccessibilityService.kt
class TaskAccessibilityService : AccessibilityService() {

    override fun onAccessibilityEvent(event: AccessibilityEvent?) {
        when (event?.eventType) {
            AccessibilityEvent.TYPE_VIEW_CLICKED -> {
                val node = event.source ?: return
                if (isLikeButton(node)) {
                    // 检测到点赞操作，通知主APP
                    sendBroadcast(Intent(ACTION_LIKE_DETECTED))
                }
            }
            AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED -> {
                // 检测是否跳转到目标App
                checkTargetApp(event.packageName?.toString())
            }
        }
    }

    override fun onInterrupt() {}
}
```

### 4.4 自动化模式切换实现

```kotlin
// viewmodel/SettingsViewModel.kt
class SettingsViewModel : ViewModel() {
    fun switchAutoMode(mode: Int) {
        when (mode) {
            0 -> { /* 手动模式：无需额外权限 */ }
            1 -> { /* 半自动：引导用户开启无障碍服务 */ }
            2 -> { /* 深度自动化：检测Root+Xposed，风险提示 */ }
        }
        DataStoreManager.setAutoMode(mode)
    }
}
```

---

## 五、管理后台开发指南

### 5.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.x | 前端框架 |
| Element Plus | 2.x | UI组件库 |
| Pinia | 2.x | 状态管理 |
| Vue Router | 4.x | 路由管理 |
| Axios | 1.x | HTTP客户端 |

### 5.2 后端API接口规范

```java
// 统一响应体
@Data
@AllArgsConstructor
@NoArgsConstructor
public class ApiResponse<T> {
    private int code;    // 200成功，其他为错误码
    private String msg;
    private T data;
    private Long timestamp = System.currentTimeMillis();
}

// 错误码规范
// 400-499 客户端错误（参数错误400、未授权401、无权限403、资源不存在404）
// 500-599 服务端错误（系统错误500、服务不可用503）
```

### 5.3 关键功能实现要点

| 功能 | 实现要点 |
|------|----------|
| 任务审核 | 列表分页查询 + 批量操作 + 审核原因填写 |
| 提现管理 | 状态机设计（待处理→审核通过/拒绝→打款中→已完成/失败）|
| 商户管理 | 子账号体系（RBAC权限模型）|
| 数据看板 | ECharts图表 + 实时数据WebSocket推送 |

---

## 六、测试与验收标准

### 6.1 功能测试覆盖

| 测试类型 | 覆盖率要求 | 工具 |
|----------|------------|------|
| 单元测试 | 后端Service层≥70% | JUnit5 + Mockito |
| 接口测试 | 核心API 100% | Postman / swagger |
| 功能测试 | 核心流程 100% | 手工 + Selenium（后台） |
| 兼容性测试 | 主流安卓设备10款 | 云测平台 |

### 6.2 核心流程验收标准

| 流程 | 验收标准 |
|------|----------|
| 注册→登录→实名认证 | 全流程≤3分钟完成，成功率≥99% |
| 任务接单→截图上传→审核→奖励到账 | 审核时效≤5分钟（AI初审），奖励到账≤1分钟 |
| 提现申请→自动打款 | ≤200元提现，到账时效≤10分钟 |
| 自动化模式切换 | 三种模式切换流畅，权限引导清晰 |

### 6.3 性能指标

| 指标 | 目标值 |
|------|--------|
| 接口P95响应时间 | < 500ms |
| 数据库查询 | < 100ms（索引优化后） |
| APP启动时间 | < 2s（冷启动） |
| 内存占用 | < 200MB（正常操作） |

---

## 七、商户子账号体系

### 7.1 功能说明

> 商户不提供独立APP，统一通过**管理后台子账号体系**登录操作。商户注册审核通过后，由平台运营在管理后台为其开通子账号（手机号+密码），商户使用该账号登录管理后台（商户专用视图），完成点数充值、任务发布、广告投放等操作。

### 7.2 RBAC权限模型

| 角色 | 权限范围 | 说明 |
|------|----------|------|
| 超级管理员 | 全部权限 | 平台运营人员 |
| 商户管理员 | 商户全部操作 | 商户主账号，可创建子账号 |
| 商户操作员 | 任务发布/查看数据 | 商户子账号，受限操作 |
| 财务 | 充值/账单查看 | 仅财务相关操作 |

### 7.3 商户子账号管理（管理后台）

```
┌─────────────────────────────────────────────────────┐
│  商户管理 > 子账号管理                              │
├─────────────────────────────────────────────────────┤
│  商户名称：xxx有限公司                              │
│  账户余额：10,000点数                              │
│                                                 │
│  [+ 新增子账号]                                   │
│                                                 │
│  账号        角色        状态      操作                │
│  138****1234  商户管理员  启用      [编辑][禁用]     │
│  139****5678  商户操作员  禁用      [编辑][启用]     │
│                                                 │
└─────────────────────────────────────────────────────┘
```

### 7.4 商户登录流程

```
商户输入账号密码 → 后端验证账号+密码+状态 → 
验证通过 → 返回JWT Token（含角色信息）→ 前端根据角色展示对应菜单
验证失败 → 返回错误信息（账号不存在/密码错误/账号已禁用）
```

---

## 八、邀请返佣系统

### 8.1 业务流程

```
用户A分享邀请码/邀请海报 → 用户B扫码注册并填写邀请码
→ 用户B完成任务获得收益 → 用户A获得B首月收益的5%作为返佣
```

### 8.2 邀请码生成规则

```kotlin
// 邀请码生成：用户ID的8位BASE62编码（不含易混淆字符0/O/1/I/L）
fun generateInviteCode(userId: Long): String {
    val chars = "23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz"
    return userId.toString(32).uppercase()
        .map { chars.indexOf(it).toString(32) }.joinToString("")
        .take(8)
}
```

### 8.3 返佣计算规则

| 参数 | 规则 |
|------|------|
| 返佣比例 | 被邀请人首月收益的5%（可配置） |
| 返佣上限 | 单次返佣最高50元 |
| 结算周期 | 每日结算前一日的返佣收益 |
| 发放方式 | 直接追加到邀请人可提现余额 |

### 8.4 数据库表（新增）

```sql
-- 邀请关系表
CREATE TABLE `t_invite_relation` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `inviter_id` BIGINT NOT NULL,
  `invitee_id` BIGINT NOT NULL,
  `invite_code` VARCHAR(16) NOT NULL,
  `status` TINYINT DEFAULT 0 COMMENT '0进行中1已完成',
  `commission_amount` DECIMAL(10,2) DEFAULT 0 COMMENT '累计返佣金额',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_inviter_id (`inviter_id`),
  INDEX idx_invitee_id (`invitee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 8.5 API接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 生成分享海报 | GET | `/api/v1/user/invite/poster` | 返回邀请海报URL（含二维码） |
| 获取邀请链接 | GET | `/api/v1/user/invite/link` | 返回邀请URL |
| 我的邀请记录 | GET | `/api/v1/user/invite/records` | 被邀请人列表及返佣明细 |
| 返佣统计 | GET | `/api/v1/user/invite/summary` | 累计返佣/待生效返佣 |

---

## 九、消息通知系统

### 9.1 通知类型与推送方式

| 通知类型 | 触发条件 | 推送方式 |
|----------|----------|----------|
| 任务审核结果 | 用户提交任务截图，AI/人工审核完成 | 站内信 + APP推送 |
| 提现审核结果 | 提现申请审核通过/拒绝 | 站内信 + APP推送 + 短信（拒绝时） |
| 新任务提醒 | 商户发布新任务，匹配用户历史偏好 | APP推送（用户可关闭） |
| 任务即将到期 | 接取的任务距截止时间不足2小时 | APP推送 |
| 收益到账提醒 | 任务审核通过，奖励到账 | 站内信 |
| 系统公告 | 平台发布全局公告 | 站内信 + 首页弹窗 |
| 申诉处理结果 | 用户提交申诉后，管理员处理完成 | 站内信 + APP推送 |

### 9.2 消息通知实现方案

| 技术方案 | 说明 |
|----------|------|
| 站内信 | 数据库存储，APP内消息中心展示 |
| APP推送 | 集成推送服务（极光推送/华为推送等） |
| 短信通知 | 调用腾讯云SMS接口，仅重要通知使用 |

### 9.3 消息中心页面设计

```
┌──────────────────────────────────┐
│  消息中心                        │
├──────────────────────────────────┤
│  [全部] [系统] [任务] [提现]     │
│                                  │
│  ● 任务审核通过                   │
│    您的任务"抖音点赞#123"审核通过…  │
│    2026-05-02 14:30              │
│                                  │
│    提现申请已处理                  │
│    您的提现申请50.0元已打款...      │
│    2026-05-02 10:15              │
│                                  │
│  [全部已读]                      │
└──────────────────────────────────┘
```

---

## 十、运营活动配置（新手任务）

### 10.1 新手任务规则

| 任务 | 奖励金额 | 触发条件 |
|------|----------|----------|
| 完成首笔任务 | 0.5元 | 注册后7天内完成第一笔任务 |
| 完成实名认证 | 0.5元 | 提交实名认证并通过 |
| 完成首次提现 | 0.5元 | 首次提现申请审核通过 |

### 10.2 活动配置后台

| 功能 | 说明 |
|------|------|
| 活动状态管理 | 启用/禁用新手任务活动 |
| 奖励金额配置 | 每个奖励金额可独立配置 |
| 活动数据看板 | 参与人数、发放奖励总额 |

### 10.3 注意事项

1. **奖励叠加**：新手任务奖励与正常任务奖励叠加发放
2. **时效限制**：注册后7天内未完成首笔任务，不再享受额外奖励
3. **防刷控制**：同一设备只能享受一次新手任务奖励

---

*文档版本：v1.1*
*更新于：2026-05-02*
*更新内容：增加商户子账号体系、邀请返佣系统、消息通知系统、运营活动配置*
*制定人：项目经理*


# 自动化任务平台 · 三期精细化阶段开发指导文档 v1.0

> 制定日期：2026-05-02（更新于2026-05-02）
> 依据文档：《PRD_自动化任务平台_v1.0.md》v1.2 + 《开发阶段计划_v1.0》
> 适用阶段：第29-36周（三期）
> 状态：✅ 开发团队执行指导文档（已根据最新决策调整）

---

## 一、三期新增功能总览

| 功能模块 | 功能点 | 优先级 | 开发工时（估算） |
|----------|--------|--------|-----------------|
| 用户等级体系 | 等级规则/权益/高价值任务解锁 | P0 | 3周 |
| 广告精准投放 | 地域/用户画像定向 | P0 | 3周 |
| **iOS版本** | **独立iOS APP开发（确定开发）** | **P0** | **5周** |
| 商户会员等级 | 积分/折扣/专属服务 | P2 | 2周 |
| 推送通知优化 | 多渠道推送整合 | P2 | 1周 |

---

## 二、用户等级体系

### 2.1 等级规则设计

| 等级 | 名称 | 累计完成任务数 | 解锁权益 |
|------|------|--------------|----------|
| Lv.1 | 初来乍到 | 0 | 默认权益 |
| Lv.2 | 小试牛刀 | 50 | 高价任务可接（奖励>5元） |
| Lv.3 | 得心应手 | 200 | 优先审核（审核时间缩短50%）|
| Lv.4 | 游刃有余 | 500 | 专属客服通道 |
| Lv.5 | 炉火纯青 | 1000 | 每日任务上限+50 |

### 2.2 等级计算规则

```kotlin
// 等级计算服务
@Service
class UserLevelService {

    fun calculateLevel(userId: Long): UserLevel {
        val completedCount = taskRecordRepository.countCompleted(userId)
        val level = when {
            completedCount >= 1000 -> Level.LV5
            completedCount >= 500 -> Level.LV4
            completedCount >= 200 -> Level.LV3
            completedCount >= 50 -> Level.LV2
            else -> Level.LV1
        }
        
        // 更新用户等级
        userRepository.updateLevel(userId, level)
        
        // 记录升级日志
        levelUpLogRepository.save(LevelUpLog(userId, level, completedCount))
        
        return level
    }
}
```

### 2.3 高价值任务权限控制

```kotlin
// 任务可接判断
fun canAcceptTask(user: User, task: Task): Boolean {
    // 基础判断：任务是否上架、是否还有名额
    if (task.status != TaskStatus.ACTIVE || task.usedQuota >= task.totalQuota) {
        return false
    }

    // 等级权限判断
    val taskLevel = determineTaskLevel(task.rewardAmount)
    if (user.level < taskLevel) {
        throw BizException("该任务需要${taskLevel.levelName}以上用户参与")
    }

    // 防刷判断：同一任务只能接一次
    if (userTaskRecordRepository.exists(user.id, task.id)) {
        throw BizException("您已接过此任务")
    }

    return true
}
```

### 2.4 用户等级展示页面（安卓端）

```kotlin
@Composable
fun LevelCenterScreen(viewModel: LevelViewModel) {
    val userLevel by viewModel.userLevel.collectAsState()
    val levelProgress by viewModel.levelProgress.collectAsState()

    Column(modifier = Modifier.padding(16.dp)) {
        // 当前等级卡片
        LevelCard(userLevel = userLevel)

        Spacer(modifier = Modifier.height(16.dp))

        // 等级进度条
        LinearProgressIndicator(
            progress = { levelProgress },
            modifier = Modifier.fillMaxWidth()
        )
        Text("${userLevel.tasks} / ${userLevel.nextLevelThreshold} 完成任务")

        Spacer(modifier = Modifier.height(24.dp))

        // 等级权益列表
        Text("等级权益", style = MaterialTheme.typography.titleMedium)
        LevelBenefitList(userLevel.benefits)
    }
}
```

---

## 三、广告精准投放

### 3.1 定向维度

| 定向维度 | 选项 | 说明 |
|----------|------|------|
| 地域定向 | 省份/城市/商圈 | 可多选，精确到城市级别 |
| 年龄定向 | 18-25 / 26-35 / 36-45 / 46+ | 可多选 |
| 设备类型 | Android / iOS | 可多选 |
| 历史行为 | 偏好抖音 / 偏好小红书 | 根据用户历史任务选择推断 |

### 3.2 精准投放引擎设计

```kotlin
// 精准投放匹配服务
@Service
class AdTargetingService {

    fun matchAds(user: User, adTypes: List<AdType>): List<Ad> {
        return adRepository.findActiveAds()
            .filter { ad ->
                // 地域匹配
                matchRegion(ad.targeting.region, user.region) &&
                // 年龄匹配
                matchAge(ad.targeting.ageRanges, user.age) &&
                // 设备匹配
                matchDevice(ad.targeting.deviceTypes, user.deviceType) &&
                // 用户与广告定向重叠度达标
                calculateMatchScore(ad.targeting, user.profile) >= ad.minMatchScore
            }
            .sortedByDescending { calculateMatchScore(it.targeting, user.profile) }
    }

    // 定向重叠度计算（简版）
    private fun calculateMatchScore(
        targeting: AdTargeting,
        profile: UserProfile
    ): Double {
        var score = 0.0
        if (targeting.region.contains(profile.region)) score += 0.3
        if (targeting.ageRanges.contains(profile.ageGroup)) score += 0.3
        if (targeting.preferredPlatform == profile.preferredPlatform) score += 0.4
        return score
    }
}
```

### 3.3 商户端定向配置页面

```
┌─────────────────────────────────────────────────┐
│  广告定向设置                                    │
├─────────────────────────────────────────────────┤
│  地域定向                                        │
│  ○ 全国（默认）                                   │
│  ● 指定地域                                      │
│    [+ 添加省份] [+ 添加城市]                      │
│                                                 │
│  年龄定向                                        │
│  ☑ 18-25岁  ☑ 26-35岁                          │
│  ☑ 36-45岁  ☐ 46岁以上                          │
│                                                 │
│  设备定向                                        │
│  ☑ Android  ☑ iOS                               │
│                                                 │
│  行为定向                                        │
│  ☑ 抖音偏好用户  ☑ 小红书偏好用户                │
│                                                 │
│  预估覆盖人数：约 12,350 人                      │
│  定向越精准，点击率/转化率越高                    │
├─────────────────────────────────────────────────┤
│              [保存定向设置]                       │
└─────────────────────────────────────────────────┘
```

---

## 四、iOS版本开发

### 4.1 技术栈选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Swift | 5.9+ | 主开发语言 |
| SwiftUI | 4.0+ | UI框架（主要）|
| UIKit | — | 复杂交互辅助 |
| Alamofire | 5.x | 网络请求 |
| Kingfisher | 7.x | 图片加载 |
| SnapKit | 5.x | AutoLayout约束 |

### 4.2 与安卓端的差异处理

| 差异点 | 安卓处理 | iOS处理 | 说明 |
|--------|----------|---------|------|
| 无障碍服务 | AccessibilityService | 无对应（App Store禁止）| iOS版仅支持手动模式 |
| 自动化模式 | 三种模式切换 | 仅手动模式 | 需在PRD中说明iOS限制 |
| 微信支付 | 调起微信APP | 调起微信APP | SDK不同，共用商户号 |
| 推送通知 | 极光/华为 | APNs | 需单独配置iOS推送证书 |
| 生物识别 | FingerprintManager | LocalAuthentication | 指纹/面容登录 |

### 4.3 iOS特有功能：面容/指纹登录

```swift
// AuthService.swift
import LocalAuthentication

class AuthService {
    func authenticateWithBiometrics() async throws -> Bool {
        let context = LAContext()
        var error: NSError?
        
        guard context.canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, error: &error) else {
            throw AuthError.biometricsUnavailable
        }
        
        return try await withCheckedThrowingContinuation { continuation in
            context.evaluatePolicy(
                .deviceOwnerAuthenticationWithBiometrics,
                localizedReason: "登录验证"
            ) { success, error in
                if success {
                    continuation.resume(returning: true)
                } else {
                    continuation.resume(throwing: error ?? AuthError.biometricsFailed)
                }
            }
        }
    }
}
```

### 4.4 iOS发布注意事项

| 事项 | 说明 |
|------|------|
| App Store审核 | 需准备隐私政策、用户协议截图 |
| 自动化限制 | 审核时需明确说明iOS版无自动化功能 |
| IDFA | 若使用广告标识符，需在隐私政策中说明 |
| 推送证书 | 开发/生产环境各需一个APNs证书 |
| TestFlight | 上架前通过TestFlight内测 |

---

## 五、商户会员等级

### 5.1 商户会员体系

| 等级 | 名称 | 累计充值金额 | 权益 |
|------|------|------------|------|
| 普通商户 | — | 0 | 默认权益 |
| 银牌商户 | 银牌 | ≥10,000元 | 平台费9折、优先审核 |
| 金牌商户 | 金牌 | ≥50,000元 | 平台费8折、专属客服、一对一培训 |
| 钻石商户 | 钻石 | ≥200,000元 | 平台费7折、VIP客服、定制化投放方案 |

### 5.2 折扣计算

```kotlin
// 商户服务费率计算
fun calculateFeeRate(merchant: Merchant): Double {
    val baseRate = 15.0 // 基础费率15%
    val discount = when {
        merchant.totalRecharge >= 200000 -> 0.7  // 7折
        merchant.totalRecharge >= 50000 -> 0.8   // 8折
        merchant.totalRecharge >= 10000 -> 0.9   // 9折
        else -> 1.0                             // 无折扣
    }
    return baseRate * discount
}
```

---

## 六、三期数据库扩展

### 6.1 用户等级表

```sql
CREATE TABLE `t_user_level` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL UNIQUE,
  `level` TINYINT NOT NULL DEFAULT 1,
  `total_tasks` INT DEFAULT 0 COMMENT '累计完成任务数',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user_id (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 等级升级日志表
CREATE TABLE `t_level_up_log` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `from_level` TINYINT,
  `to_level` TINYINT NOT NULL,
  `total_tasks` INT NOT NULL,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 6.2 商户会员表

```sql
CREATE TABLE `t_merchant_member` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `merchant_id` BIGINT NOT NULL UNIQUE,
  `level` TINYINT NOT NULL DEFAULT 0 COMMENT '0普通1银牌2金牌3钻石',
  `total_recharge` DECIMAL(14,2) DEFAULT 0 COMMENT '累计充值金额',
  `discount_rate` DECIMAL(4,2) DEFAULT 1.00 COMMENT '费率折扣',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 七、三期测试要点

### 7.1 用户等级体系测试

| 测试项 | 场景 | 验收标准 |
|--------|------|----------|
| 等级自动升级 | 用户完成任务数达到阈值 | 自动升级，发送通知 |
| 高价值任务可见性 | 低等级用户浏览高价值任务 | 灰度显示，无法接单 |
| 权益生效 | 各等级用户享受对应权益 | 折扣计算正确，审核时间缩短 |

### 7.2 精准投放测试

| 测试项 | 场景 | 验收标准 |
|--------|------|----------|
| 地域定向 | 商户设置仅投北京用户 | 非北京用户不展示 |
| 画像匹配 | 用户画像完整时 | 匹配度高的广告优先展示 |
| 覆盖率预估 | 商户配置定向后 | 预估人数与实际曝光偏差<20% |

### 7.3 iOS版本测试

| 测试项 | 场景 | 验收标准 |
|--------|------|----------|
| 基础功能 | 注册/登录/任务大厅/收益/提现 | 与安卓功能一致 |
| 自动化模式 | iOS版不展示自动化设置入口 | 功能隔离 |
| 生物识别 | 指纹/面容登录 | 识别成功率高 |
| App Store上架 | 提交审核 | 审核通过 |

---

*文档版本：v1.0*
*制定人：项目经理*
*下一步：项目收尾，输出《运营手册》和《运维文档》*

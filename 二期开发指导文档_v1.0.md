# 自动化任务平台 · 二期完善阶段开发指导文档 v1.0

> 制定日期：2026-05-02（更新于2026-05-02）
> 依据文档：《PRD_自动化任务平台_v1.0.md》v1.2 + 《开发阶段计划_v1.0》
> 适用阶段：第17-28周（二期）
> 状态：✅ 开发团队执行指导文档（已根据最新决策调整）

---

## 一、二期新增功能总览

| 功能模块 | 功能点 | 优先级 | 开发工时（估算） |
|----------|--------|--------|-----------------|
| 小红书任务 | 点赞/评论任务类型扩展 | P0 | 3周 |
| 广告大厅 | 广告展示+观看+奖励领取 | P0 | 3周 |
| 商户广告发布 | 商户端广告投放 | P0 | 2周 |
| 商户数据报表 | 数据看板+ROI分析 | P1 | 2周 |
| **运营活动配置** | **节日翻倍/邀请赛/任务冲刺** | **P1** | **2周** |
| 管理后台完善 | 数据看板+操作日志 | P2 | 1周 |

---

## 二、小红书任务扩展

### 2.1 技术要点

与一期抖音任务共用核心逻辑，仅需扩展以下内容：

| 扩展点 | 说明 |
|--------|------|
| 任务平台枚举 | 新增 `platform=2`（小红书） |
| 目标链接解析 | 小红书链接格式解析（笔记ID提取） |
| AI审核模型 | 小红书界面识别模型（与抖音模型共用接口，模型不同） |
| 自动化引导 | 新增小红书App的操作路径识别 |

### 2.2 数据表扩展

```sql
-- 无需新增表，仅需在t_task表增加小红书相关字段
ALTER TABLE `t_task` ADD COLUMN `platform` TINYINT NOT NULL DEFAULT 1 COMMENT '1抖音2小红书';
```

### 2.3 任务类型枚举扩展

```kotlin
enum class Platform(val code: Int, val appPackage: String) {
    DOUYIN(1, "com.ss.android.ugc.aweme"),
    XIAOHONGSHU(2, "com.xingin.xhs");

    companion object {
        fun fromCode(code: Int) = entries.first { it.code == code }
    }
}
```

### 2.4 开发注意事项

1. **链接有效性验证**：小红书链接需调用小红书API或第三方服务验证有效性，防止商户发布无效链接
2. **自动化引导适配**：小红书UI结构与抖音不同，需重新定义按钮识别规则（XPath/text匹配）
3. **AI审核模型**：小红书和抖音的界面特征不同，需训练/采购专门的识别模型

---

## 三、广告大厅

### 3.1 广告类型与计费

| 广告类型 | 计费模式 | 单价范围 | 用户奖励触发条件 |
|----------|-----------|----------|------------------|
| 视频广告 | CPV | 0.1-2元/次 | 观看完成率≥90% |
| 图文广告 | CPM | 5-20元/千次 | 停留时长≥30秒 |
| 落地页广告 | CPC | 0.2-5元/次 | 停留时长≥20秒 |

### 3.2 核心API设计

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 广告列表 | GET | `/api/v1/ads` | 支持类型筛选、分页 |
| 广告详情 | GET | `/api/v1/ads/{id}` | 返回素材链接、奖励金额、时长要求 |
| 标记观看 | POST | `/api/v1/ads/{id}/watch/start` | 开始观看，记录开始时间 |
| 确认完成 | POST | `/api/v1/ads/{id}/watch/complete` | 完成后调用，触发奖励 |
| 领取奖励 | POST | `/api/v1/ads/{id}/reward` | 用户领取广告奖励（防刷验证） |

### 3.3 防刷机制

| 措施 | 说明 |
|------|------|
| 时间验证 | 视频广告需实际观看≥90%时长才允许提交 |
| 行为验证 | 图文广告需检测页面滑动行为（通过JS注入） |
| 设备验证 | 同一设备每日广告观看次数上限（可配置，默认20次） |
| 奖励锁定 | 奖励到账前需通过防刷校验 |

### 3.4 广告展示设计（安卓端）

```kotlin
@Composable
fun AdDetailScreen(viewModel: AdViewModel) {
    val ad by viewModel.adDetail.collectAsState()
    val watchProgress by viewModel.watchProgress.collectAsState()

    Column {
        // 广告素材展示区
        when (ad.type) {
            AdType.VIDEO -> VideoPlayer(ad.videoUrl) // 带进度条
            AdType.IMAGE -> ImageCarousel(ad.images) // 自动轮播
            AdType.LANDING -> WebView(ad.landingUrl) // 带JS注入监控
        }

        // 防刷验证交互（答题/滑动）
        if (watchProgress >= ad.requiredProgress) {
            AntiBotVerification(onSuccess = { viewModel.claimReward() })
        }

        // 奖励展示
        Text("完成后可获得 ${ad.rewardAmount} 元")
    }
}
```

---

## 四、商户广告发布

### 4.1 广告发布流程

```
选择广告类型 → 上传素材 → 填写落地页（可选） → 设置奖励金额/预算
→ 系统预估成本 → 商户确认扣除点数 → 提交平台审核 → 上架
```

### 4.2 素材规格（复用PRD附录A）

| 类型 | 格式 | 尺寸 | 大小限制 |
|------|------|------|----------|
| 图片广告 | JPG/PNG | 1:1或16:9 | ≤2MB |
| 视频广告 | MP4 | 720P+ | ≤50MB，时长15-60s |
| 封面图 | JPG/PNG | 3:2 | ≤500KB |

### 4.3 广告计费与点数扣除

| 计费模式 | 点数扣除时机 | 说明 |
|----------|--------------|------|
| CPV（视频） | 用户完成观看并领取奖励时 | 按实际完成次数扣除 |
| CPM（图文） | 广告展示1000次时 | 按展示量扣除 |
| CPC（落地页） | 用户点击并完成目标行为 | 按有效点击扣除 |

> 实际扣除公式：`扣除点数 = 实际完成量 × 单价`

### 4.4 商户广告管理页面

| 功能 | 说明 |
|------|------|
| 广告列表 | 展示所有广告（状态/数据/预算余额） |
| 数据统计 | 展示量/点击量/完成量/CPM/CPC实时数据 |
| 预算管理 | 调整每日预算/总预算、暂停/重启 |
| 素材管理 | 替换广告素材（需重新审核） |

---

## 五、商户数据报表

### 5.1 核心指标

| 指标 | 说明 | 计算公式 |
|------|------|----------|
| 任务完成数 | 累计成功完成的任务数 | SUM(used_quota) |
| 广告曝光数 | 广告被展示的总次数 | COUNT impressions |
| 广告点击数 | 用户点击广告的次数 | COUNT clicks |
| 点数总消耗 | 商户账户累计消耗点数 | SUM(recharge - balance) |
| 任务ROI | 每消耗一点数产生的完成数 | 完成数 / 点数消耗 |
| 广告CTR | 广告点击率 | 广告点击数 / 曝光数 |

### 5.2 数据看板设计（管理后台）

```javascript
// 关键图表组件
const DashboardChart = {
  // 1. 点数消耗趋势折线图（日/周/月）
  // X轴：时间，Y轴：消耗点数
  
  // 2. 任务完成量柱状图
  // 按平台（抖音/小红书）分组
  
  // 3. ROI分析饼图
  // 任务消耗 vs 广告消耗占比
  
  // 4. Top任务排行榜
  // 按完成数排序，展示前10
}
```

### 5.3 数据导出

| 导出类型 | 格式 | 字段 |
|----------|------|------|
| 任务消耗明细 | Excel | 日期/任务ID/任务名称/完成数/消耗点数 |
| 广告消耗明细 | Excel | 日期/广告ID/广告标题/曝光量/点击量/消耗点数 |
| 日报表汇总 | Excel | 日期/充值金额/消费金额/新增商户/新增用户 |

---

## 六、邀请返佣

### 6.1 业务流程

```
用户A分享邀请码/邀请海报 → 用户B扫码注册并填写邀请码
→ 用户B完成任务获得收益 → 用户A获得B首月收益的5%作为返佣
```

### 6.2 邀请码生成规则

```kotlin
// 邀请码生成：用户ID的8位BASE62编码（不含易混淆字符0/O/1/I/L）
// 示例：用户ID=12345 → 邀请码=3I9W
fun generateInviteCode(userId: Long): String {
    val chars = "23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz"
    return userId.toString(32).uppercase()
        .map { chars.indexOf(it).toString(32) }.join()
        .take(8)
}
```

### 6.3 返佣计算规则

| 参数 | 规则 |
|------|------|
| 返佣比例 | 被邀请人首月收益的5%（可配置） |
| 返佣上限 | 单次返佣最高50元 |
| 结算周期 | 每日结算前一日的返佣收益 |
| 发放方式 | 直接追加到邀请人可提现余额 |

### 6.4 API接口

| 接口 | 路径 | 说明 |
|------|------|------|
| 生成分享海报 | GET | `/api/v1/user/invite/poster` 返回邀请海报URL（含二维码） |
| 获取邀请链接 | GET | `/api/v1/user/invite/link` 返回邀请URL |
| 我的邀请记录 | GET | `/api/v1/user/invite/records` 被邀请人列表及返佣明细 |
| 返佣统计 | GET | `/api/v1/user/invite/summary` 累计返佣/待生效返佣 |

---

## 六、运营活动配置

### 6.1 活动类型与规则

| 活动类型 | 触发条件 | 奖励规则 | 配置灵活性 |
|----------|----------|----------|--------------|
| 新手任务 | 注册后7天内 | 完成首笔任务+0.5元、实名认证+0.5元、首次提现+0.5元 | 奖励金额可配置 |
| 节日翻倍 | 指定节日期间 | 任务奖励临时翻倍（如春节、双11） | 翻倍倍数可配置 |
| 邀请赛 | 活动期间（如一个月） | 邀请最多新用户的Top10各奖励50元 | 奖励金额和Top数量可配置 |
| 任务冲刺 | 活动期间 | 10任务奖10元、30任务奖30元、50任务奖50元 | 阶梯数量和奖励可配置 |

### 6.2 活动配置后台（管理后台）

```
┌─────────────────────────────────────────────────┐
│  运营活动管理                                  │
├─────────────────────────────────────────────────┤
│  [+ 新建活动]         筛选：[全部▾] [进行中▾]   │
│                                                 │
│  活动名称       类型      时间范围        状态      │
│  春节翻倍活动   节日翻倍  2026-01-28~02-03  未开始│
│  五月邀请赛    邀请赛    2026-05-01~05-31  进行中│
│  任务冲刺      任务冲刺  2026-05-01~05-15  已结束│
│                                                 │
│  点击"春节翻倍活动"查看详情：                      │
│  活动名称：春节翻倍活动                            │
│  活动类型：节日翻倍                                │
│  翻倍倍数：2倍                                    │
│  适用任务：全部任务                                │
│  开始时间：2026-01-28 00:00                      │
│  结束时间：2026-02-03 23:59                      │
│  状态：[未开始]                                   │
│  [编辑] [启用] [删除]                            │
└─────────────────────────────────────────────────┘
```

### 6.3 活动API设计

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 活动列表 | GET | `/api/v1/activities` | 当前可进行的活动（C端） |
| 活动详情 | GET | `/api/v1/activities/{id}` | 活动规则和用户进度 |
| 领取活动奖励 | POST | `/api/v1/activities/{id}/claim` | 满足条件后领取 |
| 我的活动记录 | GET | `/api/v1/activities/my-records` | 我参与的活动及奖励 |
| **管理后台API** | | | |
| 活动创建/编辑 | POST/PUT | `/api/v1/admin/activities` | 创建或更新活动 |
| 活动列表（管理） | GET | `/api/v1/admin/activities` | 所有活动列表 |
| 活动数据看板 | GET | `/api/v1/admin/activities/{id}/stats` | 参与人数、发放奖励总额 |

### 6.4 活动配置注意事项

1. **活动冲突处理**：同一用户同一时间只能参与一个同类活动，优先级：节日翻倍 > 邀请赛 > 任务冲刺
2. **奖励发放时效**：满足活动条件后，奖励实时到账（新手任务）/ 活动结束后统一发放（邀请赛）
3. **防刷机制**：活动奖励领取需通过防刷验证，同一设备多账号参与活动将取消资格

---

## 七、二期新增数据库表

### 7.1 广告相关（3张表）

```sql
-- 广告表
CREATE TABLE `t_ad` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `merchant_id` BIGINT NOT NULL,
  `title` VARCHAR(128) NOT NULL,
  `type` TINYINT NOT NULL COMMENT '1视频2图文3落地页',
  `material_urls` VARCHAR(2048) NOT NULL COMMENT '素材链接JSON',
  `landing_url` VARCHAR(1024) COMMENT '落地页链接',
  `reward_amount` DECIMAL(10,2) NOT NULL COMMENT '用户单次奖励',
  `budget` DECIMAL(12,2) NOT NULL COMMENT '总预算',
  `spent` DECIMAL(12,2) DEFAULT 0 COMMENT '已消耗预算',
  `charge_type` TINYINT NOT NULL COMMENT '1CPV2CPM3CPC',
  `unit_price` DECIMAL(10,2) NOT NULL COMMENT '单价',
  `status` TINYINT DEFAULT 0 COMMENT '0待审核1已上架2已暂停3已结束4已拒绝',
  `reject_reason` VARCHAR(256),
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_merchant_id (`merchant_id`),
  INDEX idx_status (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 广告观看记录表
CREATE TABLE `t_ad_watch_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `ad_id` BIGINT NOT NULL,
  `start_time` DATETIME,
  `end_time` DATETIME,
  `watch_duration` INT COMMENT '实际观看时长（秒）',
  `reward_granted` TINYINT DEFAULT 0 COMMENT '是否已发放奖励',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user_id (`user_id`),
  INDEX idx_ad_id (`ad_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 活动配置表（二期新增）
CREATE TABLE `t_activity` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(128) NOT NULL,
  `type` TINYINT NOT NULL COMMENT '1新手任务2节日翻倍3邀请赛4任务冲刺',
  `rules` VARCHAR(2048) COMMENT '规则JSON',
  `start_time` DATETIME NOT NULL,
  `end_time` DATETIME NOT NULL,
  `status` TINYINT DEFAULT 0 COMMENT '0未开始1进行中2已结束',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_status (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 八、二期测试要点

### 8.1 功能测试

| 测试项 | 测试场景 | 验收标准 |
|--------|----------|----------|
| 小红书任务 | 发布→接单→审核→奖励全流程 | 与抖音任务一致 |
| 广告完整观看 | 视频广告需看完90%才触发奖励 | 中途退出不发放 |
| 运营活动 | 用户参与活动并领取奖励 | 奖励金额正确，活动规则生效 |
| 数据报表 | 导出Excel对比数据库数据 | 数据准确无误 |

### 8.2 性能测试

| 指标 | 目标值 |
|------|--------|
| 广告列表接口P95 | < 300ms |
| 数据看板首次加载 | < 3s |
| 活动配置保存 | < 1s |

---

*文档版本：v1.0*
*制定人：项目经理*
*下一步：进入三期开发准备阶段*

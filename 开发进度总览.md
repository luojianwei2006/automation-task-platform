# 自动化任务平台 · 一期MVP开发进度总览

> 更新时间：2026-05-05 | 当前阶段：第3-6周（账号体系）✅ 已完成
> 开发负责人：资深开发工程师（AI）

---

## 一、当前进度

| 阶段 | 状态 | 完成内容 |
|------|------|----------|
| **第1-2周 基础搭建** | ✅ 100% | 数据库SQL + 后端骨架 + API规范 + 认证API + 管理后台框架 + 安卓框架 |
| **第3-6周 账号体系** | ✅ 100% | 用户信息API + 实名认证 + RBAC权限 + 管理后台用户管理页 + 安卓登录/注册/实名认证UI |
| 第7-10周 任务系统核心 | ⏳ 待开始 | 任务CRUD、截图上传、AI审核、奖励发放 |
| 第11-13周 收益/提现/自动化 | ⏳ 待开始 | 收益中心、提现流程、半自动模式、邀请返佣 |
| 第14-16周 联调测试上线 | ⏳ 待开始 | E2E联调、性能测试、生产部署 |

---

## 二、已创建的文件清单

### 📁 后端项目 (`/backend/`)

```
task-platform/
├── task-common/                         # 公共模块 ✅
│   └── src/main/java/com/task/platform/common/
│       ├── response/ApiResponse.java    # 统一响应体（+msg重载方法）
│       ├── response/ErrorCode.java      # 错误码枚举 (50+错误码)
│       ├── exception/BusinessException.java
│       ├── exception/GlobalExceptionHandler.java
│       └── utils/PasswordUtil.java / JwtUtil.java
│
├── task-user-service/                   # 用户服务 ✅ (端口8081)
│   └── src/main/java/com/task/platform/user/
│       ├── UserApplication.java
│       ├── entity/User.java             # 用户实体（含auto_mode字段）
│       ├── entity/InviteRelation.java   # 邀请关系实体 🆕
│       ├── mapper/UserMapper.java
│       ├── mapper/InviteRelationMapper.java  # 累计返佣统计 🆕
│       ├── service/UserService.java     # 注册/登录/短信（Bug已修复） ✅
│       ├── service/UserProfileService.java   # 用户资料/邀请体系 🆕
│       ├── service/RealAuthService.java      # 实名认证+审核 🆕
│       └── controller/AuthController.java    # 认证6个接口
│       └── controller/UserController.java    # 用户资料/邀请 🆕
│       └── controller/RealAuthController.java  # 实名认证 🆕
│
├── task-admin-api/                      # 管理后台API ✅ (端口8083)
│   └── src/main/java/com/task/platform/admin/
│       ├── AdminApplication.java        # 启动类 🆕
│       ├── entity/AdminUser.java        # 管理员实体 🆕
│       ├── mapper/AdminUserMapper.java  🆕
│       ├── security/AdminUserDetails.java        # Spring Security Principal 🆕
│       ├── security/AdminUserDetailsService.java 🆕
│       ├── security/JwtAuthFilter.java           # JWT过滤器 🆕
│       ├── config/AdminSecurityConfig.java       # RBAC权限配置 🆕
│       ├── service/AdminAuthService.java         # 登录+子账号管理 🆕
│       ├── controller/AdminAuthController.java   # 管理员登录/账号管理 🆕
│       └── controller/AdminUserController.java   # C端用户管理+实名审核 🆕
│   └── src/main/resources/application.yml  🆕
│
├── task-task-service/                   # 任务服务 (骨架)
├── task-pay-service/                    # 支付服务 (骨架)
└── task-job/                           # 定时任务 (骨架)
```

### 📁 数据库 (`/sql/`)
- `init_database.sql` — 12张表 + **新增 `t_admin_user` 表** + 超管初始化账号

### 📁 管理后台前端 (`/frontend/admin-frontend/`)
- `src/api/user.ts` — 用户管理API封装（列表/详情/封禁/实名审核）🆕
- `src/views/user/index.vue` — **完整用户管理页面**（搜索+筛选+分页+实名审核弹窗）🆕

### 📁 安卓端项目 (`/android/`)
- `repository/AuthRepository.kt` — 认证数据仓库 🆕
- `repository/UserRepository.kt` — 用户/实名认证数据仓库 🆕
- `service/RealAuthStatus.kt` — 实名状态数据模型 🆕
- `viewmodel/LoginViewModel.kt` — 登录/注册ViewModel（含短信倒计时） 🆕
- `viewmodel/RealAuthViewModel.kt` — 实名认证ViewModel 🆕
- `ui/login/LoginScreen.kt` — **登录页UI（密码+验证码双模式）** 🆕
- `ui/login/RegisterScreen.kt` — **注册页UI（含邀请码）** 🆕
- `ui/profile/RealAuthScreen.kt` — **实名认证页UI（状态流转展示）** 🆕
- `network/ApiClient.kt` — 更新接口定义，新增用户信息相关API ✅

---

## 三、已实现的API接口

### 认证模块 (`/api/v1/auth`) — 6个接口
| 方法 | 路径 | 功能 | 状态 |
|------|------|------|------|
| POST | `/sms/send` | 发送短信（Redis三Key防刷，Bug已修复） | ✅ |
| POST | `/register` | 用户注册 | ✅ |
| POST | `/login` | 密码登录 | ✅ |
| POST | `/login/sms` | 验证码登录 | ✅ |
| POST | `/password/reset` | 重置密码 | ✅ |
| POST | `/token/refresh` | Token刷新 | ✅ |

### 用户信息模块 (`/api/v1/user`) — 6个接口（新增）
| 方法 | 路径 | 功能 | 状态 |
|------|------|------|------|
| GET | `/profile` | 获取用户资料 | ✅ |
| PUT | `/profile` | 更新昵称/头像 | ✅ |
| POST | `/wallet/bind` | 绑定微信/支付宝 | ✅ |
| GET | `/invite/link` | 获取邀请链接 | ✅ |
| GET | `/invite/records` | 我的邀请记录+返佣统计 | ✅ |
| POST | `/real-auth` | 提交实名认证 | ✅ |
| GET | `/real-auth/status` | 查询实名认证状态 | ✅ |

### 管理后台API (`/api/admin`) — 6个接口（新增）
| 方法 | 路径 | 功能 | 权限 |
|------|------|------|------|
| POST | `/auth/login` | 管理员登录 | 公开 |
| POST | `/accounts` | 创建子账号 | 超管+商户管理员 |
| PUT | `/accounts/{id}/status` | 启用/禁用账号 | 超管+商户管理员 |
| GET | `/merchant/{id}/accounts` | 商户子账号列表 | 超管+商户管理员 |
| GET | `/users` | C端用户列表（分页+筛选） | 超管+商户管理员+财务 |
| PUT | `/users/{id}/status` | 封禁/解封用户 | 超管 |
| POST | `/users/{id}/real-auth/review` | 实名审核（通过/拒绝） | 超管 |

---

## 四、关键技术决策与代码质量要点

1. **RBAC角色体系**：超管/商户管理员/商户操作员/财务 4个角色，Spring Security + `@PreAuthorize` 注解控制
2. **实名认证状态机**：0未认证→1审核中→2已通过/3失败，失败可重新提交
3. **AES加密身份证**：身份证号加密存储，API响应返回脱敏格式（前6后4）
4. **管理后台无状态**：JWT认证，Session = STATELESS，Redis DB1与用户服务隔离
5. **短信Bug修复**：`redisTemplate.expire(limitKey, 1L, TimeUnit.DAYS)` 修复编译错误
6. **短信API统一Map参数**：安卓端使用 `Map<String, Any>` 替代强类型DTO，灵活适配不同场景
7. **邀请返佣查询**：`InviteRelationMapper.sumCommissionByInviterId()` 一条SQL完成统计

---

## 五、下一步计划（第7-10周：任务系统核心）

### 后端待开发：
- [ ] 任务CRUD API（发布/列表/详情/接单/提交截图）
- [ ] 腾讯云COS文件上传（截图+身份证照片）
- [ ] AI视觉审核（腾讯云图片审核API）
- [ ] 任务奖励发放（审核通过后异步写入收益明细）
- [ ] 任务配额原子更新（Redis + DB乐观锁）

### 安卓端待开发：
- [ ] 任务大厅列表页（分页+下拉刷新+筛选Tab）
- [ ] 任务详情页（任务描述+截图上传+提交按钮）
- [ ] 截图上传功能（调相机/相册 + COS SDK上传）
- [ ] 半自动模式引导（无障碍服务开启引导弹窗）

### 管理后台待开发：
- [ ] 任务管理页面（列表+审核+拒绝）
- [ ] 数据看板（ECharts：日活/任务量/收益趋势）

### 第三方对接：
- [ ] 腾讯云SMS SDK真实发送（替换TODO占位）
- [ ] 腾讯云COS SDK（文件上传）
- [ ] 腾讯云视觉审核 API

---

## 一、当前进度

| 阶段 | 状态 | 完成内容 |
|------|------|----------|
| **第1-2周 基础搭建** | ✅ 100% | 数据库SQL + 后端骨架 + API规范 + 认证API + 管理后台框架 + 安卓框架 |
| 第3-6周 账号体系 | ⏳ 待开始 | 实名认证、管理后台权限、用户管理模块 |
| 第7-10周 任务系统核心 | ⏳ 待开始 | 任务CRUD、截图上传、AI审核、奖励发放 |
| 第11-13周 收益/提现/自动化 | ⏳ 待开始 | 收益中心、提现流程、半自动模式、邀请返佣 |
| 第14-16周 联调测试上线 | ⏳ 待开始 | E2E联调、性能测试、生产部署 |

---

## 二、已创建的文件清单

### 📁 后端项目 (`/backend/`)

```
task-platform/
├── pom.xml                              # 父POM (Spring Boot 3.2.0, Java 17)
├── task-common/                         # 公共模块 ✅
│   ├── pom.xml
│   └── src/main/java/com/task/platform/common/
│       ├── response/ApiResponse.java    # 统一响应体
│       ├── response/ErrorCode.java      # 错误码枚举 (50+错误码)
│       ├── exception/BusinessException.java  # 业务异常类
│       ├── exception/GlobalExceptionHandler.java  # 全局异常处理
│       └── utils/
│           ├── PasswordUtil.java        # BCrypt密码工具
│           └── JwtUtil.java             # JWT工具 (JJWT 0.12+)
├── task-user-service/                   # 用户服务 ✅ (含认证API)
│   ├── pom.xml
│   └── src/main/java/com/task/platform/user/
│       ├── UserApplication.java         # 启动类 (端口8081)
│       ├── entity/User.java             # 用户实体
│       ├── mapper/UserMapper.java       # MyBatis-Plus Mapper
│       ├── service/UserService.java     # 核心业务: 注册/登录/验证码/刷新Token
│       └── controller/AuthController.java  # 6个RESTful API端点
│   └── src/main/resources/
│       └── application.yml              # 配置文件 (Druid+Redis+MP)
├── task-task-service/                   # 任务服务 (骨架)
├── task-pay-service/                    # 支付服务 (骨架)
├── task-admin-api/                      # 管理后台API (骨架)
└── task-job/                            # 定时任务 (骨架)
```

### 📁 数据库 (`/sql/`)
- `init_database.sql` — 12张表完整建表脚本 + 初始化数据 + 用户授权

### 📁 管理后台前端 (`/frontend/admin-frontend/`)
- Vue 3 + TypeScript + Vite + Element Plus 2.x + Pinia + Vue Router 4
- ✅ 登录页（渐变背景 + 表单校验）
- ✅ Layout布局（侧边栏菜单 + 顶部导航）
- ✅ Axios封装（拦截器 + Token携带 + 统一错误处理）
- ✅ Pinia用户状态管理（登录态持久化）
- ✅ 6个页面占位：Dashboard / 用户 / 任务 / 提现 / 商户 / 设置
- ✅ 构建验证通过（507ms编译）

### 📁 安卓端项目 (`/android/`)
- Kotlin 1.9 + Jetpack Compose + MVVM + Hilt + Retrofit + DataStore
- ✅ Gradle构建配置（compileSdk 34, minSdk 24）
- ✅ 数据模型层（ApiResponse/LoginResponse/TaskDTO/EarningsSummary）
- ✅ 网络层（Retrofit+OkHttp拦截器+Token自动注入）
- ✅ DataStore本地存储封装
- ✅ TaskAccessibilityService（无障碍服务 - 半自动模式核心）
- ✅ accessibility_service_config.xml（正确注册配置）
- ✅ AndroidManifest.xml（权限声明 + 服务注册）
- ✅ Navigation导航图（6个路由节点）

---

## 三、已实现的API接口

### 认证模块 (`/api/v1/auth`) — 6个接口已实现

| 方法 | 路径 | 功能 | 状态 |
|------|------|------|------|
| POST | `/sms/send` | 发送短信验证码（Redis双Key防刷） | ✅ |
| POST | `/register` | 用户注册（密码+验证码+邀请码） | ✅ |
| POST | `/login` | 密码登录 | ✅ |
| POST | `/login/sms` | 验证码登录 | ✅ |
| POST | `/password/reset` | 重置密码 | ✅ |
| POST | `/token/refresh` | Token刷新（过期7天内可换新） | ✅ |

---

## 四、关键技术决策与代码质量要点

1. **金额精度**：所有金额字段使用 `DECIMAL(10,2)`，Java侧用 `BigDecimal`
2. **密码安全**：BCrypt加密（10轮salt），禁用明文存储
3. **JWT规范**：JJWT 0.12+ 写法 `Keys.hmacShaKeyFor()`，30天有效期 + RefreshToken机制
4. **短信防刷**：Redis 双Key设计（验证码60s有效 + 每日10次限制 + 60s冷却）
5. **全局异常处理**：`@RestControllerAdvice` 捕获所有异常返回标准格式
6. **参数校验**：`@Valid` + `@NotBlank` 注解驱动
7. **手机号脱敏**：API响应中手机号显示为 `138****1234`
8. **无障碍服务**：正确配置XML + AndroidManifest注册，支持抖音/小红书点赞检测
9. **前端安全**：Axios拦截器统一处理401跳转登录

---

## 五、下一步计划（第3-6周：账号体系）

### 后端待开发：
- [ ] 实名认证对接（腾讯云实人认证API）
- [ ] 商户子账号体系（RBAC权限模型）
- [ ] Spring Security集成（管理后台鉴权）
- [ ] 用户信息查询/更新API

### 安卓端待开发：
- [ ] 注册/登录/实名认证完整UI页面
- [ ] 手机号输入框、倒计时验证码按钮
- [ ] 实名认证拍照上传页面

### 管理后台待开发：
- [ ] Spring Security RBAC角色权限体系
- [ ] 用户管理CRUD页面（列表/详情/封禁）
- [ ] 角色权限分配功能

### 第三方对接准备：
- [ ] 腾讯云SMS SDK集成
- [ ] 微信支付API v3 SDK接入
- [ ] 腾讯云AI视觉审核SDK
